# Fix the relocation comparison transition

## Changes
- Replace the form immediately after “Compare the two cities” is clicked with a dedicated progress screen.
- Keep the existing calculation and advisor request unchanged; show the results screen when it finishes.
- If comparison fails, return to the form and preserve the existing error message.

## Verification
- Click the comparison button in the live preview and confirm the progress screen appears immediately.
- Confirm the completed relocation results still replace it and the page remains error-free.
